# Dependências apenas para desenvolvimento / CI.
# Uso: `pip install -r requirements.txt -r requirements-dev.txt`
#   ou `uv pip install -r requirements-dev.txt --python .venv/bin/python`
pytest==9.0.3
pytest-asyncio==1.3.0
